@model MVC_Imagens.Models.Produto

@{
    ViewBag.Title = "Detalhes do Produto";
}

<h1>Detalhes</h1>
<hr />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        @Html.HiddenFor(m => m.ProdutoId)

        <div class="form-group">
            @Html.LabelFor(m => m.Nome, new { @class = "control-label col-md-2" })
                <label class="control-label">@Model.Nome</label> 
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Descricao, new { @class = "control-label col-md-2" })
            <label class="control-label">@Model.Descricao</label>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Preco, new { @class = "control-label col-md-2" })
            <label class="control-label">@Model.Preco</label>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Categoria.CategoriaNome, new { @class = "control-label col-md-2" })
            <label class="control-label">@Model.Categoria.CategoriaNome</label>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-2">
                <img src="@Model.Imagem" alt="@Model.Nome image" class="img-thumbnail" />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-2">
                @Html.ActionLink("Editar", "Edit", new { id = @Model.ProdutoId }, new { @class = "btn btn-success" })
                @Html.ActionLink("Voltar", "Index", null, new { @class = "btn btn-danger" })
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
